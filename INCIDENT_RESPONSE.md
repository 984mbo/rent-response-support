# 公開インシデント対応方針

RENT RESPONSEプロジェクト（984mbo）は、サービス障害、個人情報の意図しない露出、認可不備、暗号化・削除処理の失敗、AI・PDF生成の異常利用をインシデントとして扱います。

## 優先度

- **SEV-1**：第三者による案件データ閲覧、秘密値漏えい、外部送信、広範な個人情報漏えいの疑い
- **SEV-2**：削除・暗号化・認可・Turnstileの重大な失敗、継続的なサービス停止、費用急増
- **SEV-3**：限定的な機能不全、誤表示、データ更新遅延、回避策のある障害

## 受付時の注意

非公開報告にも、通知原本、氏名・住所・家賃・契約情報、Cookie・token、案件ID、PDF・ZIP、秘密値を記載・添付しないでください。発生日時（JST）、ページURL、エラーコード、CF-Ray、個人情報を除いた再現手順だけを受け付けます。非公開報告はGitHub上に保存され、リポジトリ管理者が確認します。

## 対応体制

- インシデント責任者：優先度、停止・復旧、対外説明を決定する。
- Cloudflare技術担当：Pages、Workers、D1、R2、Durable Objects、Queue、AI、Turnstileを調査・封じ込めする。
- プライバシー判断担当：影響データと法令上の報告・本人通知の要否を確認する。
- 公開連絡担当：個人情報や攻撃手順を除いた障害情報を公開する。
- 代替担当：主担当が応答できない場合に引き継ぐ。

少人数運営では兼務できますが、誰が各役割を担当したかを記録します。重大報告の24時間以内一次確認を実際に維持できるか、公開前と定期的な演習で確認します。

## 対応手順

1. 受領内容を公開情報と非公開情報に分離し、時刻・影響範囲・報告経路を記録する。
2. 必要に応じて実ファイル受付を停止し、WorkerまたはPagesを直前の正常版へ戻し、漏えいの可能性がある秘密値をローテーションする。
3. D1、R2、Durable Objects、Workers AI、Browser Rendering、Turnstile、Pagesの監査情報を、通知本文・氏名・住所を通常ログへ転記せず確認する。
4. 影響を受ける案件、期間、データ種別、第三者アクセスまたは外部送信の有無を判定する。
5. 修正をテスト環境で検証し、段階的に復旧する。復旧後もrate limit、Turnstile、費用、エラー率を監視する。
6. 利用者に必要な行動がある場合は、公開Issueで障害概要と安全な行動を案内する。個別案件を特定する情報は掲載しない。
7. 原因、検知、対応時間、影響、再発防止を振り返り、機密情報を除いた事後報告を公開する。

記録には発覚、対応開始、封じ込め、復旧の時刻（JSTとUTC）、commit・deployment ID、影響期間・件数・データ種別、暗号鍵露出・第三者アクセス・外部送信の有無、意思決定者を残します。通知原本、本文、Cookie・token、秘密値、生の案件IDはインシデント記録へコピーしません。

## 初動時の安全な操作

- 新規受付を停止：`REAL_FILE_UPLOAD_ENABLED=false` を配備する。
- AI費用・Bot急増：Turnstile設定、AI rate limit、Cloudflare利用量を確認し、必要なら受付を停止する。
- 認可・暗号化の疑い：新規受付を停止し、`APP_KEK_B64`やTurnstile secretを直ちに公開せずローテーション計画を作る。暗号鍵を変更する前に既存案件の復旧可能性と24時間削除を確認する。
- 外部送付の疑い：本番設定の`DISPATCH_PROVIDER=none`、D1送付投影、Queue consumerを確認する。
- Turnstile障害：新規作成・アップロードだけをfail closedで停止し、既存案件のダウンロードと削除は維持する。
- Pages・workers.devの誤公開：対象routeを停止するか、既知の正常deploymentへ戻す。
- TTL・DLQ障害：新規受付を止め、tombstone、R2 lifecycle、Durable Object alarm、DLQを確認する。

## 復旧条件

復旧前に、Turnstileの本番action・hostname・単回token、改ざんcapabilityと越境Originの拒否、R2暗号化と削除、`PREPARED_NOT_SENT`、ログの個人情報不在、AI利用量、Queue/DLQ、PDF・ZIPのSHA整合、非機微のJPEG/PDFによるE2Eを確認します。

公開前と重要変更時に、Turnstile停止、秘密鍵ローテーション、暗号鍵侵害の疑い、TTL失敗を机上演習します。個人データ漏えい等の報告・本人通知が必要かは、[個人情報保護委員会の案内](https://www.ppc.go.jp/personalinfo/legal/leakAction/)を基に責任者が必要に応じて専門家と判断します。

## 公開連絡

障害概要、開始時刻、影響する機能、暫定回避策、復旧状況を `incident` ラベルのIssueで更新します。脆弱性の詳細、攻撃手順、秘密値、通知画像、氏名、住所、案件IDは公開しません。
